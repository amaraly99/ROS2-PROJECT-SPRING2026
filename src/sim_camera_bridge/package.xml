<?xml version="1.0"?>
<package format="3">
  <name>sim_camera_bridge</name>
  <version>1.0.0</version>
  <description>HIL only — subscribe to a synthetic ROS image topic, write NV12 frames into the /ovcam_frames POSIX SHM that yolo_producer + ovcam_bridge already consume. Replaces ovcam_producer in HIL mode.</description>
  <maintainer email="b00095536@aus.edu">ahmed</maintainer>
  <license>MIT</license>
  <buildtool_depend>ament_cmake</buildtool_depend>
  <depend>rclcpp</depend>
  <depend>sensor_msgs</depend>
  <depend>cv_bridge</depend>
  <export><build_type>ament_cmake</build_type></export>
</package>
