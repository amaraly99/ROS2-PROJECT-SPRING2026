# bench_pbvs.yaml — TS4 controller gains (visp_pbvs_servo PBVS).
# Pass AFTER bench_fsm.yaml.
/**:
  ros__parameters:
    lambda: 0.3    # lateral depth-scaled gain: vy = clamp(-lambda * tx_metric, +/-1.5)
    k_fwd:  3.0    # forward bbox-ratio gain: vx = k_fwd * (target_ratio - bbox_ratio)
