# bench_pbvs.yaml — TS4 controller gains (visp_pbvs_servo PBVS).
# Pass AFTER bench_fsm.yaml.
/**:
  ros__parameters:
    lambda: 0.5        # PBVS gain: v = -lambda * L^-1 * e
    sign_width: 0.6    # physical sign width (m) — used for solvePnP 3D corners
